/**
 * Wraps the serial port interface manager
 * 
 */

//var factory = require( 'cs=controller');

function PortManager()
{
    
}

// Makes PortManager object globally accessible within the sails application
module.exports = new PortManager;

